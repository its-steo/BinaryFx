// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // THIS DISABLES TURBOPACK
  // Next.js will use Webpack (stable)
  // Remove all "turbo" flags
}

module.exports = nextConfig